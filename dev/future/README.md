Local-only holding area for future or experimental plugin work.

This folder lives under `dev/`, which is already gitignored, so anything here
stays out of the public repo until it proves itself.

Current use:
- archived `Full` preset experiments
- local planning scratch space for future preset or masking work
